
import { Blog } from '@/screens';
import { View } from 'react-native';

const News = () => {
  return (
    <View style={{ 
      flex: 1,
      width: '100%',
    }}>
      <Blog />
    </View>
  );
};

export default News;
