
import { AgentsScreen } from '@/screens';
import { View } from 'react-native';

const Agents = () => {
  return (
    <View style={{ 
      flex: 1,
      width: '100%',
    }}>
      <AgentsScreen />
    </View>
  );
};

export default Agents;
