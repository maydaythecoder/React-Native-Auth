
import { View } from 'react-native';
import { Home as HomeScreen } from '@/screens';

const Index = () => {
  return (
    <View style={{ 
      flex: 1,
      width: '100%',
    }}>
      <HomeScreen />
    </View>
  );
};

export default Index;
