
import { HomeScreen } from '@/screens';
import { View } from 'react-native';

function Home() {
  return (
    <View style={{ 
      flex: 1,
      width: '100%',
    }}>
      <HomeScreen />
    </View>
  );
};

export default Home;
