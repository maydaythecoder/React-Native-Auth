
import { Home } from '@/screens';
import { View } from 'react-native';

const HomeScreen = () => {
  return (
    <View style={{ 
      flex: 1,
      width: '100%',
    }}>
      <Home />
    </View>
  );
};

export default HomeScreen;
